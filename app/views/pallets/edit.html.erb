<h1>Editing Pallet</h1>
<%= form_with(model: @pallet, local: true) do |form| %>
  <% if @pallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pallet.errors.count, "error") %> prohibited this pallet from being saved:</h2>

      <ul>
      <% @pallet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'Cantidad recibida' %>
    <%= form.number_field :origin_qty %>
  </div>

  <div class="field">
    <%= form.label 'Fecha de elaboración' %>
    <%= form.date_select :elab_date %>
  </div>

  <div class="field">
    <%= form.label 'Fecha de vencimiento' %>
    <%= form.date_select :exp_date %>
  </div>

  <div class="field">
    <%= form.label 'Lote' %>
    <%= form.text_field :batch %>
  </div>

  <div class="actions">
    <%= form.submit 'Editar Pallet' %>
  </div>
<% end %>

