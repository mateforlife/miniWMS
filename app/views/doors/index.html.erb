<p id="notice"><%= notice %></p>

<h1>Andenes</h1>

<table>
  <thead>
    <tr>
      <th>Número</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @doors.each do |door| %>
      <tr>
        <td><%= door.number %></td>
        <!--<td><%= link_to 'Show', door %></td>-->
        <!--<td><%= link_to 'Edit', edit_door_path(door) %></td>-->
        <td><%= link_to 'Destroy', door, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Door', new_door_path %>
